import { Albert_Sans } from 'next/font/google';
import { Lusitana } from 'next/font/google';

export const albert_sans = Albert_Sans({ subsets: ['latin'] });
export const lusitana = Lusitana({
  subsets: ['latin'],
  weight: ['400', '700'],
});
